import { QueryStatus } from "@/enum/query-enum";

export const queryStatus = [
    QueryStatus.CLOSED,
    QueryStatus.CONTACTED,
    QueryStatus.IN_PROGRESS,
    QueryStatus.NEW,
    QueryStatus.SPAM
]